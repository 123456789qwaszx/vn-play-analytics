# vn-play-analytics — PLAN

작성일: 2026-09-11  
상태: 구현 전 / v1  
목적: VN 플레이 기록 수집·통계를 통해 JPA와 SQL을 학습한다.

## 1. 프로젝트 목표

**챕터와 선택지를 등록하고 → 회차에서 선택 기록을 수집하고 → 장면별 선택 횟수와 비율을 조회한다.**

`spring-prepare/dev`에서 익숙한 VN 도메인과 통계 질문을 가져온다. 새 레포 `vn-play-analytics`에서 학습 목적에 맞게 모델과 API를 새로 구성한다. 기존 서버와 동일한 API·스키마를 유지하는 이식 작업은 아니다.

- `spring-prepare`: 실제 게임의 저장·복구·멀티 디바이스 연동 프로젝트로 유지한다.
- `game-spring-basic-assignment`: 입문 주차 과제이다.
- `vn-play-analytics`: JPA 저장·연관관계·SQL 집계·조회 개선을 공부하는 별도 프로젝트이다.
- 숙련 주차의 공식 발제문은 아직 확인되지 않았다. 이 문서는 개인 학습 계획이며 과제 요구사항을 대신하지 않는다. 확정되지 않은 주차 날짜나 제출 기한을 가정하지 않는다.

### 완료했을 때 설명할 수 있어야 하는 것

1. 엔티티 관계와 DB의 PK·FK·UNIQUE가 어떻게 연결되는가.
2. 요청 DTO, 엔티티, 응답 DTO를 왜 나누는가.
3. Service의 트랜잭션과 JPA 변경 감지가 어떤 작업을 수행하는가.
4. 선택 기록을 어떤 단위로 수집하고 어떤 분모로 집계하는가.
5. 동일한 질문을 SQL과 JPQL로 표현하면 무엇이 달라지는가.
6. JOIN으로 행이 늘어날 때 집계가 어떻게 잘못될 수 있는가.
7. 실제 SQL을 보고 N+1과 불필요한 데이터 조회를 어떻게 찾는가.

## 2. 범위

| 구분 | 내용 |
| --- | --- |
| 첫 완성 범위 M0~M5 | 챕터·장면·선택지 등록, 회차 시작·종료, 선택 기록 수집·조회, 선택 횟수·비율, SQL/JPQL 결과 비교 |
| 후속 학습 M6 | 조회 SQL 관찰, 페이징, N+1 개선 실험, 인덱스와 실행 계획 |
| 선택 확장 | 이벤트 기록·도달률, 사용자별 통계 |
| 제외 | 로그인·JWT·Spring Security·인가·멀티 디바이스·세이브/로드·이어하기·북마크·회차 포크 |
| 제외 | revision·요청 지문·재전송 보장·행 잠금·낙관적 잠금·동시성 테스트 |
| 보류 | JSON 원본 보관·스냅샷·체크섬·콘텐츠 버전·복합 PK·Redis·WebSocket·배포 |

일반적인 쓰기 트랜잭션은 유지한다. 동시성·복구 프로토콜을 추가하지 않는다. 첫 구현에는 `@Version`을 넣지 않는다.

## 3. 개발 환경과 구현 원칙

### 환경

- Java 21, Gradle Wrapper, Spring Boot, Spring Data JPA, Validation, MySQL을 사용한다.
- Spring Boot의 구체 버전은 M0에서 교육 환경과 맞춰 확정하고 기록한다. Hibernate 버전은 Boot 의존성 관리에 따르며 별도로 임의 지정하지 않는다.
- MySQL은 기존 Docker 사용 경험을 활용한다. 개발 DB `vn_analytics`, 테스트 DB `vn_analytics_test`를 분리한다.
- 자동 테스트도 MySQL을 기준으로 한다. H2와 MySQL의 차이를 동시에 해결하는 일을 첫 목표에 추가하지 않는다.
- 테스트 실행 방법과 DB 준비 방법은 README에 기록한다. 비밀번호는 환경변수 또는 Git 제외 설정에 둔다.
- M0에서 학습용 스키마 생성 방식을 하나 정해 기록한다. 초기에는 JPA DDL 생성으로 시작할 수 있고, Flyway 도입은 별도 학습으로 미룬다. 같은 스키마를 여러 초기화 방식으로 중복 관리하지 않는다.

### 코드 원칙

- Controller → Service → Repository의 흐름을 유지한다.
- 기능별 패키지: `content`, `playthrough`, `choice`, `stats`, `common`.
- 패키지 내부 클래스가 많아지기 전에는 불필요하게 세분화하지 않는다.
- 요청/응답 DTO는 record를 사용할 수 있다. 엔티티를 API 응답으로 직접 반환하지 않는다.
- 연관관계는 우선 자식 → 부모 단방향 `ManyToOne`으로 시작한다. 조회용이라는 이유만으로 모든 부모에 컬렉션을 추가하지 않는다.
- 연관관계의 로딩 전략을 명시하고, 실제 실행 SQL로 확인한다.
- 양방향 관계·cascade·orphanRemoval은 필요한 생명주기 사례가 생길 때 검토한다.
- 첫 버전에는 콘텐츠 수정·삭제 API를 만들지 않는다. 기록이 가리키는 선택지의 의미를 유지한다.
- 쓰기는 Service 트랜잭션으로 묶고, 조회는 필요한 범위에서 읽기 전용 트랜잭션을 사용한다.
- CRUD는 JPA, 통계는 SQL로 기대 결과를 먼저 확인한 뒤 JPQL과 비교한다. 모든 통계를 네이티브 SQL로 고정하지 않는다.
- 구현 안내는 한 마일스톤씩 진행한다. 한 단계의 검증과 이해를 마친 뒤 다음 단계로 간다.

## 4. 최소 데이터 모델

첫 버전은 5개 엔티티로 구성한다. 모든 PK는 단일 `Long id`이다.

| 엔티티 / 테이블 | 주요 필드 | 관계·제약 |
| --- | --- | --- |
| Chapter / chapters | id, code, title | code UNIQUE, 비어 있지 않음 |
| Episode / episodes | id, chapter_id, code, title | Chapter FK, UNIQUE(chapter_id, code) |
| ChoiceOption / choice_options | id, episode_id, option_index, label | Episode FK, UNIQUE(episode_id, option_index), 인덱스 0 이상 |
| Playthrough / playthroughs | id, chapter_id, started_at, ended_at | Chapter FK, ended_at NULL이면 진행 중 |
| ChoiceRecord / choice_records | id, playthrough_id, choice_option_id, recorded_at | Playthrough·ChoiceOption FK, 추가 기록 방식 |

`ChoiceRecord`는 세이브 슬롯을 거치지 않고 회차에 직접 속한다. 선택지를 통해 장면과 챕터를 찾으므로 처음에는 그 ID를 기록 테이블에 중복 저장하지 않는다.

### 수집 규칙

- 회차 하나는 챕터 하나에 속한다.
- 등록된 선택지만 기록할 수 있다.
- 선택지가 속한 챕터와 회차의 챕터가 같아야 한다.
- 진행 중인 회차만 선택을 기록할 수 있다.
- 같은 회차에서 같은 장면·선택지를 반복 선택하면 새로운 기록을 추가한다.
- 요청 재전송도 새 기록으로 처리한다. 정확히 한 번 수집을 보장하지 않는다. 이 제한을 README에 명시한다.
- `recorded_at`은 서버에서 기록을 받은 시각이다. 오프라인 선택 시각은 첫 버전에서 받지 않는다.
- 선택 순서가 필요하면 기록 ID 오름차순을 사용한다. 클라이언트 seq 발급은 도입하지 않는다.
- 회차 종료는 ended_at을 설정한다. 이미 종료한 회차의 종료 요청은 처음 종료 시각을 유지한다.

### 통계 규칙

- 선택 횟수의 단위는 `ChoiceRecord` 행이다. 고유 사용자 수나 회차별 마지막 선택이 아니다.
- 선택 비율의 분모는 **해당 장면의 전체 선택 기록 수**이다.
- 선택되지 않은 옵션도 횟수 0, 비율 0.0으로 응답한다.
- 기록이 전혀 없는 장면도 등록된 옵션 목록과 0 값을 응답한다.
- 비율은 소수 첫째 자리로 반올림한다. 반올림 결과의 합은 정확히 100.0이 아닐 수 있다.
- 존재하지 않는 장면은 404이다. 존재하지만 기록이 없는 경우와 구분한다.
- 첫 버전에서 ended_at은 회차 종료 여부만 뜻한다. 이를 VN 스토리 완주율로 부르지 않는다.

## 5. API 초안

| 단계 | 메서드·경로 | 목적 | 정상 응답 |
| --- | --- | --- | --- |
| M1 | POST /chapters | 챕터와 장면·선택지 일괄 등록 | 201, 생성 ID·Location |
| M1 | GET /chapters | 챕터 요약 목록 | 200 |
| M1 | GET /chapters/{chapterId} | 장면·선택지를 포함한 콘텐츠 조회 | 200 |
| M2 | POST /chapters/{chapterId}/playthroughs | 회차 시작 | 201, 생성 ID·Location |
| M2 | GET /playthroughs/{playthroughId} | 회차 상태 조회 | 200 |
| M2 | POST /playthroughs/{playthroughId}/end | 회차 종료 | 200, 종료 상태 |
| M3 | POST /playthroughs/{playthroughId}/choices | optionId 한 건 수집 | 201, 기록 ID·시각 |
| M4 | GET /playthroughs/{playthroughId}/choices?page=0&size=20 | 기록 페이지 조회 | 200 |
| M5 | GET /stats/episodes/{episodeId}/choices | 옵션별 횟수·비율 | 200 |

M1의 등록 요청은 챕터 내부 장면 1개 이상, 각 장면의 선택지 2개 이상으로 제한한다. 첫 모델의 Episode는 선택이 발생하는 장면만 다룬다. 전체 VN 장면 종류를 표현하려 하지 않는다.

- 등록 요청 내 중복 장면 code, 중복 optionIndex는 400으로 처리한다.
- 이미 등록된 Chapter code와 충돌하면 409로 처리한다.
- 비어 있는 필수 문자열·잘못된 페이지 인자·다른 챕터의 선택지는 400이다.
- 존재하지 않는 챕터·회차·선택지·장면은 404이다.
- 종료 회차에 기록을 추가하면 409이다.
- 오류 응답은 `{code, message}`로 통일한다. 검증 오류 상세 목록은 필요할 때 추가한다.
- 기록 페이지는 ID 오름차순, size 1~100으로 제한하고 items, page, size, totalElements, totalPages를 응답한다.

## 6. 마일스톤

### M0. 새 프로젝트와 DB 준비

**목표:** 재현 가능한 실행 환경을 만든다.

- [ ] 새 레포와 Spring 프로젝트를 준비한다. 이 PLAN 작성 자체는 레포 생성 완료를 의미하지 않는다.
- [ ] 패키지 루트를 `com.vnanalytics`로 정하고 의존성 버전을 기록한다.
- [ ] 개발·테스트 DB 연결과 스키마 생성 방식을 설정한다.
- [ ] 실행·테스트 명령, 환경변수 예시를 README에 적는다.
- [ ] PLAN.md를 새 레포에 포함한다.

**완료 기준:** 앱 기동과 MySQL 연결을 확인한다. 테스트가 개발 DB를 사용하지 않는다.

**설명 질문:** JPA, Hibernate, Spring Data JPA는 각각 무엇을 맡는가? DB 스키마는 누가 생성하는가?

### M1. 콘텐츠 등록과 조회

**목표:** Chapter·Episode·ChoiceOption의 관계를 구현한다.

- [ ] 엔티티·PK·FK·UNIQUE 제약을 정의한다.
- [ ] 챕터와 장면·선택지를 한 요청으로 등록한다.
- [ ] Service 트랜잭션에서 부모부터 저장한다. 첫 구현에 cascade는 필수가 아니다.
- [ ] 목록 DTO와 상세 DTO를 구분한다.
- [ ] 필수값·중복값 검증과 공통 예외 응답을 구성한다.
- [ ] 개발용 샘플 콘텐츠를 등록하고 DB 행과 API 응답을 비교한다.

**검증:** 정상 등록·상세 조회, 중복 거부, 등록 실패 후 일부 데이터만 남지 않음을 확인한다. 롤백 검증은 실제 쓰기 후 실패하는 상황으로 확인하고, 저장 전 검증 실패만으로 롤백을 검증했다고 하지 않는다.

**설명 질문:** FK를 가진 쪽은 어디인가? 부모 컬렉션 없이 관계를 표현할 수 있는가? 엔티티와 응답 DTO가 다른 이유는 무엇인가?

### M2. 회차 시작과 종료

**목표:** 엔티티 조회와 변경 감지를 학습한다.

- [ ] 챕터를 지정해 회차를 시작한다.
- [ ] 회차 단건 조회와 종료를 구현한다.
- [ ] 엔티티에 종료 메서드를 두고 트랜잭션 안에서 호출한다.
- [ ] 종료 후 DB 갱신을 확인하고 추가 save 호출의 필요 여부를 설명한다.

**검증:** 회차 생성, 없는 챕터 404, 종료 시각 반영, 반복 종료 시 첫 시각 유지.

**설명 질문:** 변경 감지는 언제 반영되는가? ended_at이 의미하는 것은 스토리 완주인가, 회차 종료인가?

### M3. 선택 기록 수집

**목표:** 관계가 있는 데이터를 올바른 회차에 기록한다.

- [ ] optionId를 받는 요청 DTO를 만든다.
- [ ] 회차와 선택지를 조회하고 챕터 일치·회차 진행 상태를 검사한다.
- [ ] ChoiceRecord를 저장하고 기록 ID와 시각을 반환한다.
- [ ] 동일 선택을 두 번 보내면 두 기록이 생성되는 현재 계약을 명시한다.

**검증:** 정상 수집, 없는 자원 404, 다른 챕터 선택 400, 종료 회차 409, 반복 선택의 누적.

**설명 질문:** 어떤 검증은 FK로 보장되고 어떤 검증은 Service가 맡는가? 반복 선택과 재전송을 현재 모델이 구분하는가?

### M4. 회차별 기록 조회

**목표:** 정렬·페이징·조회 DTO를 구현한다.

- [ ] 기록 ID 오름차순 페이지 조회를 구현한다.
- [ ] 응답에 기록 ID·장면 정보·선택지 정보·기록 시각을 포함한다.
- [ ] 존재하는 빈 회차는 빈 페이지, 없는 회차는 404로 구분한다.
- [ ] 상세 정보 조립 시 발생하는 SQL을 관찰한다.

**검증:** 첫/다음/빈 페이지, 정렬 순서, 다른 회차 기록이 섞이지 않음, size 범위.

**설명 질문:** 페이지 본문 조회와 전체 건수 조회는 어떤 SQL을 만드는가? 관계 객체를 읽을 때 추가 조회가 발생하는가?

### M5. 선택 통계 — SQL과 JPQL 비교

**목표:** 동일한 집계 기준을 두 접근으로 구현한다. 이 단계까지가 첫 완성이다.

- [ ] 옵션 3개의 장면과, 선택 횟수 3·1·0인 고정 데이터를 준비한다.
- [ ] SQL로 옵션별 횟수를 구한다. 옵션을 기준으로 LEFT JOIN하여 0건 옵션을 유지한다.
- [ ] COUNT(*)와 COUNT(기록 ID)의 차이를 확인한다.
- [ ] SQL 집계에서 기대 비율 75.0·25.0·0.0을 확인한다.
- [ ] 동일한 횟수 집계를 JPQL DTO projection으로 구현한다.
- [ ] JPQL 결과의 작은 집계 목록에서 분모와 비율을 계산한다. 전체 원시 기록을 Java에 가져와 세지 않는다.
- [ ] API는 기본적으로 JPQL 구현을 사용하고, SQL은 비교 쿼리로 보관한다. API를 두 개로 늘리지 않는다.
- [ ] 두 접근의 최종 응답이 같은지 비교한다. 필요하면 SQL의 횟수 결과를 같은 비율 계산에 통과시켜 비교한다.

**검증:** 3·1·0, 전체 0건, 다른 장면·챕터 기록 제외, 같은 회차의 반복 선택 반영, 반올림 규칙.

**설명 질문:** 분모는 무엇인가? 왜 LEFT JOIN인가? 여러 1:N을 한꺼번에 JOIN하면 집계가 왜 부풀 수 있는가? SQL과 JPQL에서 이름이 가리키는 대상은 무엇인가?

### M6. 조회 개선 실험

**목표:** 관찰된 문제 한 가지를 근거로 개선한다. M5 이후 진행한다.

- [ ] 같은 조건의 샘플을 늘려 M4 또는 콘텐츠 상세 조회의 쿼리 수를 측정한다.
- [ ] N+1이 관찰되면 DTO projection, fetch join, EntityGraph, 배치 조회 중 맞는 한 가지를 적용한다.
- [ ] N+1이 없으면 의도적인 비교 실습을 별도 커밋에서 할 수 있다. 실제 코드에 반드시 문제가 있다고 가정하지 않는다.
- [ ] 개선 전후 결과가 같고 추가 쿼리가 줄었는지 확인한다.
- [ ] 통계 SQL의 실행 계획을 읽고 기존 인덱스를 확인한 후 필요한 인덱스만 검토한다.
- [ ] 컬렉션 fetch join과 페이징을 무작정 결합하지 않는다. 실제 조회 대상과 페이지 단위를 먼저 확인한다.

**완료 기준:** 문제·측정 조건·변경·결과·한계를 README에 한 사례로 설명한다. 작은 데이터의 실행 시간만으로 성능 향상을 단정하지 않는다.

## 7. 테스트와 샘플 데이터

실제 동작 계약을 확인하는 Service/API 통합 테스트와 집계 테스트를 우선한다. 단순 getter·setter나 Repository 위임만 확인하는 테스트는 만들지 않는다.

| 데이터 | 목적 |
| --- | --- |
| 챕터 A·B | 다른 챕터의 선택 거부·통계 혼입 방지 |
| A의 장면 E1·E2 | 장면별 분모 분리 |
| E1의 옵션 3개 | 3·1·0 집계와 0건 옵션 표시 |
| 진행 회차 2개·종료 회차 1개 | 회차별 조회·종료 후 기록 거부 |
| 기록 없는 장면·회차 | 없음과 0건 구분 |

- seed는 새 스키마에 맞춰 만든다. 기존 seed를 수정 없이 복사하지 않는다.
- 테스트 DB를 명시하고 개발 DB에서 데이터 정리 작업을 실행하지 않는다.
- 테스트는 단계마다 필요한 범위만 실행하고, M5에서 전체 흐름을 검증한다.
- 첫 버전에서 인증·충돌·멀티 디바이스 테스트를 가져오지 않는다.
- CI 도입은 로컬의 실행·테스트 명령이 안정된 뒤 별도로 판단한다.

## 8. 기존 프로젝트에서 참고할 것

분석 기준: `123456789qwaszx/spring-prepare`, `dev`의 `46af1976faf1bb8d6dbcc8095d5ddce2880201b3`.

| 기존 자료 | 활용 방법 |
| --- | --- |
| choice_ratio.sql | 에피소드별 분모·라벨·정렬 기준 참고. 새 모델에서는 선택지 테이블로 조회하여 JSON_TABLE을 제거한다. |
| event_reach.sql | 선택 확장의 이벤트 도달률 기준 참고 |
| SaveHistoryService | 기록 조회 책임의 분리 참고 |
| Service·Repository 구조 | 트랜잭션 경계와 SQL/JPA 비교에 참고 |
| seed·통계 테스트 | 기대 결과를 아는 데이터 설계 방식 참고 |

기존 통계의 결과 의미를 무조건 그대로 가져오지 않는다. 새 모델에서 분모·반복 선택·종료의 의미를 이 PLAN 기준으로 다시 정의한다.

참고 링크:

- [기존 프로젝트](https://github.com/123456789qwaszx/spring-prepare/tree/46af1976faf1bb8d6dbcc8095d5ddce2880201b3)
- [기존 선택 통계 SQL](https://github.com/123456789qwaszx/spring-prepare/blob/46af1976faf1bb8d6dbcc8095d5ddce2880201b3/src/main/resources/sql/stats/choice_ratio.sql)
- [기존 저장 v3 계약 — 이번 범위와 구분용](https://github.com/123456789qwaszx/spring-prepare/blob/46af1976faf1bb8d6dbcc8095d5ddce2880201b3/docs/client-save-v3.md)

## 9. 선택 확장 — 첫 완성 이후

### E1. 이벤트 기록과 도달률

- EventRecord를 회차와 이벤트 코드에 연결한다.
- 반복 보고를 여러 행으로 받을지, 회차당 한 번만 남길지 먼저 정한다.
- 도달률은 챕터 범위의 전체 회차 중 해당 이벤트에 도달한 서로 다른 회차의 비율로 정의한다.
- 반복 기록으로 분자가 늘지 않도록 COUNT DISTINCT를 학습한다.

### E2. 사용자별 통계

- 로그인 없는 User(id, username)와 회차의 사용자 참조를 추가한다.
- 사용자별 회차 수·선택 수를 구한다.
- 여러 1:N JOIN에서의 팬아웃을 재현하고 올바른 집계 방식과 비교한다.

복합키·JSON 매핑·콘텐츠 버전·@Version은 추가 학습 후보이다. E1·E2 또는 기술 후보를 자동으로 본 계획의 필수 범위에 편입하지 않는다.

## 10. 진행 관리와 인수인계

| 단계 | 상태 | 결과 기록 |
| --- | --- | --- |
| M0 환경 | 미착수 | 버전·실행·DB |
| M1 콘텐츠 | 미착수 | 관계·등록·조회 |
| M2 회차 | 미착수 | 변경 감지·종료 |
| M3 수집 | 미착수 | 검증·누적 |
| M4 기록 조회 | 미착수 | 페이지·SQL |
| M5 통계 | 미착수 | SQL/JPQL 비교 |
| M6 개선 | 선택 / 미착수 | 측정 전후 |

- 커밋은 완료한 동작 단위로 나눈다. `feat:`, `test:`, `refactor:`, `docs:` 등 기존에 익숙한 접두사를 사용한다.
- 단계가 끝나면 변경 내용·실행한 검증·남은 문제·다음 단계를 기록한다.
- 공식 과제가 나오면 필수 기술과 본 계획을 비교하고 우선순위를 조정한다. 공식 과제에 이 프로젝트를 제출할 수 있다고 가정하지 않는다.
- 다른 채팅에서 이어갈 때 이 PLAN, 현재 브랜치·커밋, 완료 단계, 최근 테스트 결과를 전달한다.

**다음 작업은 M0이다. 문서 작성 시점에는 새 레포 생성·코드 구현·테스트 실행이 완료되지 않았다.**
